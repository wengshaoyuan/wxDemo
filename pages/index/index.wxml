<!--index.wxml-->

<view class="section">
  <picker mode="region" bindchange="bindRegionChange" value="{{region}}" custom-item="{{customItem}}">
    <view class="picker">
      你要选择的是：{{region[0]}}，{{region[1]}}，{{region[2]}}
    </view>
  </picker>
 <navigator hover-class="other-navigator-hover" url="../../pages/show/show" >跳转到新页面</navigator>
</view>
